export enum COLOR_TOPIC {
  BACKGROUND = "background",
  ACCENT = "accent",
  TEXT = "text",
  LINE = "line",
}

export enum THEMES {
  DARK = "dark",
  OCEAN_WAVE = "ocean_wave",
  ROSE_GOLD = "rose_gold",
  CUSTOM = "custom",
}
